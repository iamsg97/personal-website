apiVersion: v1
kind: Service
metadata:
  name: portfolio-app-service
  labels:
    app: portfolio-app
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: portfolio-app
